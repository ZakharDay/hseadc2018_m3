<p>
  <strong>Name:</strong>
  <%= link_to product.name, category_product_path(product.category, product) %>
</p>

<p>
  <strong>Description:</strong>
  <%= product.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= product.price %>
</p>

<% if controller_name == 'categories' %>
  <%= link_to 'Edit', edit_category_product_path(product.category) %>
<% else %>
  <%= link_to 'Edit', edit_product_path(product) %>
<% end %>
